import { Theme } from '@mui/material/styles';
export declare const tabsStyles: ({ palette, breakpoints }: Theme) => never;
export declare const tabItemStyles: ({ palette, breakpoints }: Theme) => never;
