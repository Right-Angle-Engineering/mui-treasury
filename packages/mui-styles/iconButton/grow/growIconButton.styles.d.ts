import { Theme } from '@mui/material/styles';
export declare type GrowIconButtonStyleProps = {
    color?: string;
    thickness?: number;
};
export declare type GrowIconButtonClassKey = keyof ReturnType<typeof growIconButtonStyles>;
declare const growIconButtonStyles: ({ palette }: Theme) => never;
export default growIconButtonStyles;
