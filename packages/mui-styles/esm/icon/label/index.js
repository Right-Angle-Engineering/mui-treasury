import makeStyles from '@mui/styles/makeStyles';
import labelIconStyles from './labelIcon.styles';
var useLabelIconStyles = makeStyles(labelIconStyles);
export { labelIconStyles, useLabelIconStyles };
export default labelIconStyles;