import { Theme } from '@mui/material/styles';
export declare type ZoomNavigationMenuStyleProps = {
    gutter?: string | number;
};
export declare type ZoomNavigationMenuClassKey = keyof ReturnType<typeof zoomNavigationMenuStyles>;
declare const zoomNavigationMenuStyles: ({ palette, spacing }: Theme) => never;
export default zoomNavigationMenuStyles;
