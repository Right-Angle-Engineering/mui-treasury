import { Theme } from '@mui/material/styles';
export declare type FloatNavigationMenuStyleProps = {
    gutter?: string | number;
};
export declare type FloatNavigationMenuClassKey = keyof ReturnType<typeof floatNavigationMenuStyles>;
declare const floatNavigationMenuStyles: ({ palette, spacing }: Theme) => never;
export default floatNavigationMenuStyles;
