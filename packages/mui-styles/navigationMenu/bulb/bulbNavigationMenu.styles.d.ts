import { Theme } from '@mui/material/styles';
export declare type BulbNavigationMenuStyleProps = {
    gutter: string | number;
};
export declare type BulbNavigationMenuClassKey = keyof ReturnType<typeof bulbNavigationMenuStyles>;
declare const bulbNavigationMenuStyles: ({ spacing, palette }: Theme) => never;
export default bulbNavigationMenuStyles;
