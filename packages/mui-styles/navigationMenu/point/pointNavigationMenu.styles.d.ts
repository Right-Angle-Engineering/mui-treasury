import { Theme } from '@mui/material/styles';
export declare type PointNavigationMenuStyleProps = {
    gutter?: string | number;
};
export declare type PointNavigationMenuClassKey = keyof ReturnType<typeof pointNavigationMenuStyles>;
declare const pointNavigationMenuStyles: ({ palette, spacing }: Theme) => never;
export default pointNavigationMenuStyles;
